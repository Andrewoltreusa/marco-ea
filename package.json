{
  "name": "marco-ea",
  "version": "0.1.0",
  "private": true,
  "description": "Marco — Oltre Castings & Design's company secretary (Trigger.dev tasks behind a Vercel route in oltre-dashboard).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "trigger:login": "trigger.dev login",
    "trigger:dev": "trigger.dev dev",
    "trigger:deploy:staging": "trigger.dev deploy --env staging",
    "trigger:deploy:prod": "trigger.dev deploy --env prod"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "trigger.dev": "4.4.3",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@trigger.dev/sdk": "4.4.4",
    "@upstash/redis": "^1.37.0"
  }
}